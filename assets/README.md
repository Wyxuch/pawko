# assets/

Drop your files here:

- `video.mp4`        — the main looping hero video
- `video-poster.jpg` — thumbnail shown before the video loads
- Replace placeholder grid items in index.html with real `<img>` tags pointing to your files
